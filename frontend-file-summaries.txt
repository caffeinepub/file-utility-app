{
  "frontend/src/utils/compression.ts": "Client-side image compression via Canvas API and PDF compression via pdf-lib CDN import; fixes Uint8Array BlobPart type error by casting `pdfBytes.buffer as ArrayBuffer`.",
  "frontend/src/utils/conversion.ts": "Client-side file format conversion utilities using Canvas API and pdf-lib; fixes Uint8Array BlobPart type errors by casting `pdfBytes.buffer as ArrayBuffer`, and improves pdf.js page type definitions.",
  "frontend/src/utils/pdfMerger.ts": "PDF merging utility using pdf-lib from CDN; fixes Uint8Array BlobPart type error by casting `pdfBytes.buffer as ArrayBuffer` when creating the output Blob.",
  "frontend/src/utils/metadataScrubber.ts": "Metadata scrubbing utility that strips EXIF from images via canvas redraw and clears PDF metadata via pdf-lib; fixes Uint8Array BlobPart type error by casting `pdfBytes.buffer as ArrayBuffer`.",
  "frontend/src/App.tsx": "Main app shell updated to use `LoginHeader` instead of `AppHeader`; `DailyUsageBadge` now reads `isPro` from context and shows \"Unlimited (Pro)\" for Pro users; all other layout, tabs, ads, and footer remain unchanged.",
  "frontend/src/components/SubscriptionPanel.tsx": "Updated subscription panel that reads live Pro status from `useGetIsPro` and shows \"Current Plan: Pro ✓\" when the user is Pro; displays a login prompt with a Login button for unauthenticated users; preserves all existing UTR submission and verification flow logic.",
  "frontend/src/components/Toolbox/Toolbox.tsx": "Toolbox shell component with a three-tool selector (PDF Merger, Background Remover, Metadata Scrubber) that renders the active tool inside a GlassCard; now accepts and forwards an `onLimitReached` callback prop to each child tool component.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for all backend operations; adds `useGetIsPro` (fetches caller's Pro status, enabled only when authenticated) and `useGetDailyUsage` (fetches daily usage response, enabled only when authenticated) alongside the existing UTR verification hooks.",
  "frontend/src/components/AdminPanel.tsx": "Admin-only panel at `/admin` that fetches and displays pending UTR submissions with email, UTR ID, timestamp, and principal; each row has an Approve button that calls `approveUTR`; shows access-denied message for non-admins and a refresh button.",
  "frontend/src/components/LoginHeader.tsx": "New header component that replicates AppHeader's glassmorphism design and adds Internet Identity login/logout UI in the top-right corner; shows a truncated principal ID pill and Logout button when authenticated, or a Login button when not.",
  "frontend/src/contexts/UsageContext.tsx": "Updated UsageContext that syncs `usesCount` and `isPro` from the backend when the user is authenticated (using `getDailyUsage` and `getIsPro` on login), falls back to localStorage for anonymous users, and calls `incrementDailyUsage` on the backend when authenticated; exposes `isPro` in context so Pro users bypass the daily limit."
}